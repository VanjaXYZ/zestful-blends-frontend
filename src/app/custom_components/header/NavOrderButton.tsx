import { Button } from "@/components/ui/button";
import React from "react";

const NavOrderButton = () => {
  return (
    <Button className="bg-primary-yellow text-black font-bold hover:bg-primary-yellow-dark-shade">
      Order Now
    </Button>
  );
};

export default NavOrderButton;
