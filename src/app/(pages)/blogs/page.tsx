import React from "react";
import BlogList from "./components/BlogList";

const page = () => {
  return (
    <main className="bg-primary-olive">
      <BlogList />
    </main>
  );
};

export default page;
